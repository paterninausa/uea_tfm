# Dependencias Python del pipeline (entorno venv + pip)
#
# Requiere Java 17 o superior (21 LTS recomendado) para PySpark 4.x —
# gestionado vía SDKMAN, ver .sdkmanrc en la raiz del repo. venv no
# gestiona JDK, asi que SDKMAN es la unica fuente de Java (sin conflicto
# de PATH con ningun JDK instalado por un gestor de entornos Python).
#
# Instalacion:
#   python3 -m venv .venv
#   source .venv/bin/activate
#   pip install -r pipeline/requirements.txt
#
# Snapshot reproducible completo (con hashes) en pipeline/requirements.lock.txt
# una vez generado con: pip freeze > pipeline/requirements.lock.txt

python-dotenv==1.0.1
pytest==8.3.3

pyspark==4.2.0            # motor de procesamiento (Structured Streaming, DataFrame API)
# pyarrow NO se fija aqui a proposito: llega como dependencia transitiva
# de pyspark y pip resuelve la version compatible automaticamente.

paho-mqtt==2.1.0          # cliente MQTT para el simulador
psycopg2-binary==2.9.9    # PostgreSQL / TimescaleDB
fastavro==1.9.7           # serializacion Avro
requests==2.32.3          # llamadas REST a Apicurio Schema Registry
kafka-python==2.0.2       # cliente Kafka (testing/debug, no para el job de Spark)
